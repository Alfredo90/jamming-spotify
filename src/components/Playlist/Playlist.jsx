import React from "react";

const Playlist = () => {
  return (
    <div>
      <input type="text" />
      <button type="submit">Save to Spotify</button>
    </div>
  );
};

export default Playlist;
